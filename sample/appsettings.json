{
  "KeyVaultName": "default invalid value, needs to be overridden by env specific settings",
  "AppConfig": {
    "Intconfig": 1,
    "StringConfig": "this is a string config defined in appsettings.json"
  }
}